# Rate limits transcribed from the provider's own documentation — see provenance. Quoted, not derived.
generated: '2026-09-20'
method: searched
source: https://docs.gitlab.com/rate_limits/
sources:
- https://docs.gitlab.com/rate_limits/
provenance:
  tool: planning/api-economics/harvest_ratelimits.py
  run: 20260920T133210
  grounding: every limit_text was found verbatim in the fetched page text
published: true
note: Overview page linking to specific rate limit docs; only two numeric limits stated (AI action and
  tag creation).
limit_count: 2
limits:
- name: AI action (ai_action_api_rate_limit)
  limit_text: 160 calls per 8 hours per authenticated user
  limit: 160
  scope: user
  endpoint: GraphQL aiAction mutation
- name: Create tag rate limit (tags_create_limit)
  limit_text: 100 requests per 30 minutes per project
  limit: 100
  scope: unclear
  endpoint: REST API endpoint for creating a tag, the GraphQL tagCreate mutation, tag creation in the
    UI, and the /tag quick action

naftiko: 1.0.0-alpha2
info:
  label: GitLab API — jira_connect_subscriptions
  description: 'GitLab API — jira_connect_subscriptions. 1 operations. Lead operation: Subscribe a namespace to a JiraConnectInstallation.
    Self-contained Naftiko capability covering one Gitlab Ci business surface.'
  tags:
  - Gitlab Ci
  - jira_connect_subscriptions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITLAB_CI_API_KEY: GITLAB_CI_API_KEY
capability:
  consumes:
  - type: http
    namespace: gitlab-ci-jira-connect-subscriptions
    baseUri: https://gitlab.com
    description: GitLab API — jira_connect_subscriptions business capability. Self-contained, no shared references.
    resources:
    - name: api-v4-integrations-jira_connect-subscriptions
      path: /api/v4/integrations/jira_connect/subscriptions
      operations:
      - name: postapiv4integrationsjiraconnectsubscriptions
        method: POST
        description: Subscribe a namespace to a JiraConnectInstallation
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: postApiV4IntegrationsJiraConnectSubscriptions
          in: body
          type: string
          required: true
  exposes:
  - type: rest
    namespace: gitlab-ci-jira-connect-subscriptions-rest
    port: 8080
    description: REST adapter for GitLab API — jira_connect_subscriptions. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/api/v4/integrations/jira-connect/subscriptions
      name: api-v4-integrations-jira-connect-subscriptions
      description: REST surface for api-v4-integrations-jira_connect-subscriptions.
      operations:
      - method: POST
        name: postapiv4integrationsjiraconnectsubscriptions
        description: Subscribe a namespace to a JiraConnectInstallation
        call: gitlab-ci-jira-connect-subscriptions.postapiv4integrationsjiraconnectsubscriptions
        with:
          postApiV4IntegrationsJiraConnectSubscriptions: rest.postApiV4IntegrationsJiraConnectSubscriptions
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: gitlab-ci-jira-connect-subscriptions-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitLab API — jira_connect_subscriptions. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: subscribe-namespace-jiraconnectinstallation
      description: Subscribe a namespace to a JiraConnectInstallation
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: gitlab-ci-jira-connect-subscriptions.postapiv4integrationsjiraconnectsubscriptions
      with:
        postApiV4IntegrationsJiraConnectSubscriptions: tools.postApiV4IntegrationsJiraConnectSubscriptions
      outputParameters:
      - type: object
        mapping: $.

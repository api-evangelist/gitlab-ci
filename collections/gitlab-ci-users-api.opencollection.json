{
  "opencollection": "1.0.0",
  "info": {
    "name": "GitLab access_requests users API",
    "version": "v4"
  },
  "items": [
    {
      "info": {
        "name": "users",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Return the user specific counts",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "{{baseUrl}}/api/v4/user_counts"
          },
          "docs": "Assigned open issues, assigned MRs and pending todos count"
        },
        {
          "info": {
            "name": "Create a runner owned by currently authenticated user",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "{{baseUrl}}/api/v4/user/runners"
          },
          "docs": "Create a new runner"
        }
      ]
    }
  ],
  "bundled": true
}